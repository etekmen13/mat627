model = "gpt-5.3-codex"
model_reasoning_effort = "high"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
service_tier = "fast"
review_model = "gpt-5.3-codex"

[tools]
web_search = { context_size = "medium" }

# Optional MCP examples can be added below if you decide they materially help.
# Keep repo-local instructions and style examples as the main source of truth.
